node_modules
.env
.env.local
.env.development.local
.dev-auth.json
dist
.DS_Store
playwright-report/
test-results/
test-results.json
SMOKE_TEST_RESULTS.txt

# Local Netlify folder
.netlify

# Coverage reports (regenerated by vitest --coverage)
coverage/

# Playwright MCP runtime artifacts
.playwright-mcp/

# Ad-hoc audit / runtime scripts dropped in repo root
audit-runtime*.mjs
audit-runtime*.js

# Loose screenshot dumps at repo root (Playwright debug runs etc.)
/*.png

# Local theme/visual audit artifact dumps (screenshots, not code)
/.light-theme-audit/

